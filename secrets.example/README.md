Copy this folder and rename it to secrets/:
   cp -r secrets.example/ secrets/
Then open each file and replace the placeholder with any value you want.
The passwords just need to match between containers - anything works for local testing.